version: 0.2

phases:
  build:
    commands:
      - echo Building Docker image
      - docker build -t myapp ./myapp
  post_build:
    commands:
      - echo Running container
      - docker run myapp
